<!doctype html>
<html>
  <head>
    <title><%= title %></title>
    <%- newrelic %>
    <link type="image/png" href="//www.wix.com/favicon.ico" rel="shortcut icon">
    <link rel="stylesheet" href="<%= staticsBaseUrl %>app<% if (!debug) { %>.min<% } %>.css">
    <style>
      @font-face {
        font-family: Doom;
        src: url("<%= staticsBaseUrl %>/assets/fonts/DooM.ttf");
      }
      body {
        margin: 0;
        background-color: black;
      }
      #root {
        display: flex;
        justify-content: center;
      }
    </style>
  </head>
  <body>
    <div id="root"></div>
    <script>
      window.__BASEURL__ = '<%= basename %>';
      window.__LOCALE__ = '<%= locale %>';
      window.__STATICS_BASE_URL__ = '<%= staticsBaseUrl %>';
    </script>
    <script src="<%= staticsBaseUrl %>app.bundle<% if (!debug) { %>.min<% } %>.js"></script>
  </body>
</html>
